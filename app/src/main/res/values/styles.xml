<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- <item name="android:background">@android:color/white</item> -->
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="android:actionBarTabStyle">@style/AppTheme.ActionBarTabs</item>
        <item name="android:actionBarTabTextStyle">@style/AppTheme.ActionBarTabs.Text</item>

        <!-- Support library compatibility -->
       <!-- <item name="background">@android:color/white</item> -->
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="actionBarTabStyle">@style/AppTheme.ActionBarTabs</item>
        <item name="actionBarTabTextStyle">@style/AppTheme.ActionBarTabs.Text</item>
    </style>

    <style name="Splash" parent="android:Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- ActionBar Styles -->
    <style name="AppTheme.ActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@android:color/holo_red_dark</item>
        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.Text</item>

        <!-- Support library compatibility -->
        <item name="background">@android:color/holo_red_dark</item>
        <item name="titleTextStyle">@style/AppTheme.ActionBar.Text</item>
    </style>

    <!-- ActionBar title text -->
    <style name="AppTheme.ActionBar.Text"
        parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>

    <!-- ActionBar tabs styles -->
    <style name="AppTheme.ActionBarTabs"
        parent="@style/Widget.AppCompat.ActionBar.TabView">
        <!-- tab indicator -->
        <item name="android:background">@drawable/actionbar_tab_indicator</item>

        <!-- Support library compatibility -->
        <item name="background">@drawable/actionbar_tab_indicator</item>
    </style>

    <!-- ActionBar tabs text -->
    <style name="AppTheme.ActionBarTabs.Text"
        parent="@style/Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@android:color/white</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>
</resources>
